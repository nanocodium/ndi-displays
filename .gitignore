.gradle/
build/
run/
out/
.idea/
.vscode/
*.iml
eclipse/
*.log
# Keep the wrapper itself — only the downloaded distributions are transient.
!gradle/wrapper/gradle-wrapper.jar

# Dev-only third-party mod jars (MCEF for the web terminal). Not ours to redistribute —
# see README for where to get them.
libs/

# VitePress wiki (source is tracked; build output and deps are not)
docs/node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/
